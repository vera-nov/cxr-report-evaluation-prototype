# cxr-report-evaluation-prototype
Прототип специализированной метрики качества текстовых описаний рентгенограмм.

Запуск скрипта оценки:
```
python evaluate_reports.py --candidates candidates.json --ground_truth ground_truth.json --language ru --output results.json
```
